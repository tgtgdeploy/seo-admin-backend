# SEO管理系统架构说明

## 系统架构概览

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         互联网用户 & 搜索引擎                          │
└────────────────────────────┬────────────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  主站点访问   │    │ 蜘蛛池访问    │    │ 跳转站点访问  │
│ (Vercel)     │    │ (9个VPS)     │    │ (Vercel)     │
└──────┬───────┘    └──────┬───────┘    └──────┬───────┘
       │                   │                   │
       │ API调用           │ Nginx反向代理      │ 301跳转
       │                   │                   │
       └───────────────────┼───────────────────┘
                           │
                           ▼
              ┌────────────────────────┐
              │   Admin Backend        │
              │   (宝塔VPS + PM2)      │
              │   adminseohub.xyz      │
              │   Port: 3100           │
              │                        │
              │   核心API:             │
              │   - /api/p/[domain]    │← 蜘蛛池内容API
              │   - /api/public/posts  │← 公开文章API
              │   - /api/websites      │← 网站管理API
              │   - /api/spider-pool   │← 蜘蛛池管理API
              │   - /dashboard         │← 管理界面
              └────────────┬───────────┘
                           │
                           │ Prisma ORM
                           │
                           ▼
              ┌────────────────────────┐
              │   PostgreSQL Database  │
              │   (Supabase)           │
              │                        │
              │   核心表:              │
              │   - Website            │← 网站配置
              │   - DomainAlias        │← 蜘蛛池域名
              │   - Post               │← 文章内容
              │   - SpiderPoolSource   │← 内容源
              │   - SpiderPoolPage     │← 蜘蛛池页面
              │   - SpiderLog          │← 爬虫日志
              │   - Keyword            │← 关键词
              │   - User               │← 用户
              └────────────────────────┘
```

---

## 三种站点类型详解

### 1. Admin后台（管理中心）

**域名**: adminseohub.xyz
**部署**: 宝塔面板VPS + PM2
**端口**: 3100
**技术栈**: Next.js 14 + Prisma + PostgreSQL

**功能模块**:

```
Admin Backend
├── 🔐 认证系统 (NextAuth.js)
│   ├── 邮箱密码登录
│   ├── 角色权限控制 (ADMIN/EDITOR/VIEWER)
│   └── Session管理
│
├── 📊 管理界面 (/dashboard)
│   ├── 网站管理 (/websites)
│   │   ├── 添加/编辑网站
│   │   ├── API密钥管理
│   │   └── 域名别名管理
│   │
│   ├── 内容管理 (/posts)
│   │   ├── 文章创建/编辑
│   │   ├── SEO优化建议
│   │   └── 文章同步
│   │
│   ├── 蜘蛛池管理 (/spider-pool)
│   │   ├── 内容源管理
│   │   ├── 页面管理
│   │   └── 爬虫日志
│   │
│   ├── 关键词管理 (/keywords)
│   │   ├── 关键词追踪
│   │   └── 排名监控
│   │
│   └── 系统设置 (/settings)
│       ├── OpenAI配置
│       └── Google Analytics配置
│
└── 🔌 API服务
    ├── /api/p/[domain] (核心蜘蛛池API)
    │   ├── 动态生成HTML页面
    │   ├── Sitemap生成
    │   ├── robots.txt生成
    │   ├── 爬虫识别和日志记录
    │   └── 访问统计
    │
    ├── /api/public/posts (公开文章API)
    │   ├── GET /api/public/posts
    │   ├── GET /api/public/posts/:slug
    │   └── API Key认证
    │
    ├── /api/websites (网站管理API)
    ├── /api/domains (域名管理API)
    ├── /api/spider-pool (蜘蛛池管理API)
    └── /api/health (健康检查)
```

**核心API: `/api/p/[domain]`**

这是整个系统的核心，为蜘蛛池提供动态内容。

```typescript
// 请求流程
GET /api/p/autopushnetwork.xyz?slug=page-0001

1. 解析域名和slug参数
   ↓
2. 查询DomainAlias表获取域名配置
   ↓
3. 识别User-Agent（是否为爬虫）
   ↓
4. 根据slug查询SpiderPoolPage表
   ↓
5. 生成HTML响应
   ↓
6. 记录访问日志到SpiderLog表
   ↓
7. 更新访问统计
   ↓
8. 返回HTML内容（缓存1小时）
```

---

### 2. 主站点（内容展示）

**域名**: telegramtghub.com, telegramupdatecenter.com, telegramtrendguide.com
**部署**: Vercel
**技术栈**: Next.js + Tailwind CSS

**架构特点**:

```
主站点
├── 静态页面 (SSG)
│   ├── 首页
│   ├── 关于页
│   └── 联系页
│
├── 动态内容 (SSR/ISR)
│   ├── 文章列表页 (从Admin API获取)
│   ├── 文章详情页 (从Admin API获取)
│   └── 分类/标签页
│
└── SEO优化
    ├── Meta标签
    ├── Sitemap
    ├── robots.txt
    └── 结构化数据 (JSON-LD)
```

**数据流**:

```typescript
// 主站点页面组件
export async function generateStaticParams() {
  // 从Admin API获取文章列表
  const response = await fetch(
    `${process.env.NEXT_PUBLIC_ADMIN_API_URL}/api/public/posts`,
    {
      headers: {
        'x-api-key': process.env.ADMIN_API_KEY!,
      },
    }
  )
  const posts = await response.json()
  return posts.map(post => ({ slug: post.slug }))
}

export default async function PostPage({ params }) {
  // 从Admin API获取文章详情
  const post = await fetchPost(params.slug)
  return <ArticleView post={post} />
}
```

**部署流程**:

```
1. 推送代码到GitHub
   ↓
2. Vercel自动触发构建
   ↓
3. 构建时调用Admin API获取文章
   ↓
4. 生成静态HTML页面
   ↓
5. 部署到Vercel CDN
   ↓
6. 用户访问时从CDN获取（极快）
```

---

### 3. 蜘蛛池站点（SEO优化）

**域名**: 9个（分布在3个VPS）
**部署**: Nginx反向代理
**目的**: 吸引搜索引擎爬虫，提升主站点SEO

**工作原理**:

```
蜘蛛池域名 (autopushnetwork.xyz)
       ↓
    Nginx配置
       ↓
location / {
    proxy_pass https://adminseohub.xyz/api/p/autopushnetwork.xyz?slug=$uri;
}
       ↓
Admin Backend API
       ↓
查询数据库获取页面内容
       ↓
返回HTML给Nginx
       ↓
Nginx缓存1小时
       ↓
返回给用户/爬虫
```

**Nginx配置示例**:

```nginx
server {
    listen 80;
    server_name autopushnetwork.xyz;

    # 主页
    location = / {
        proxy_pass https://adminseohub.xyz/api/p/autopushnetwork.xyz?slug=index;
        proxy_cache_valid 200 1h;
    }

    # 所有页面
    location / {
        set $slug $uri;
        proxy_pass https://adminseohub.xyz/api/p/autopushnetwork.xyz?slug=$slug;
        proxy_cache_valid 200 1h;
    }

    # Sitemap
    location = /sitemap.xml {
        proxy_pass https://adminseohub.xyz/api/p/autopushnetwork.xyz?type=sitemap;
        proxy_cache_valid 200 24h;
    }

    # robots.txt
    location = /robots.txt {
        proxy_pass https://adminseohub.xyz/api/p/autopushnetwork.xyz?type=robots;
        proxy_cache_valid 200 24h;
    }
}
```

**页面生成策略**:

```
SpiderPoolPage表结构:
├── pageNum: 1-1000 (页面编号)
├── slug: "page-0001" (URL slug)
├── title: AI生成或模板生成
├── content: 从SpiderPoolSource随机组合
├── keywords: 关键词标签
├── theme: 主题分类 (auto, content, crawl等)
└── views: 访问统计
```

**SEO优化**:

1. **内链网络**: 每个页面链接到其他页面，形成内链网络
2. **关键词密度**: 合理分布关键词
3. **内容更新**: 定期更新页面内容
4. **爬虫友好**:
   - 清晰的HTML结构
   - 快速响应时间
   - Sitemap提交
5. **主站链接**: 页面中包含指向主站的链接

---

## 数据库设计

### ER图

```
Website (网站)
├─ id
├─ name
├─ domain
├─ apiKey
└─ domainAliases []
   │
   ├──→ DomainAlias (蜘蛛池域名)
   │    ├─ id
   │    ├─ domain
   │    ├─ primaryTags []
   │    ├─ secondaryTags []
   │    └─ spiderPoolPages []
   │        │
   │        └──→ SpiderPoolPage (蜘蛛池页面)
   │             ├─ id
   │             ├─ pageNum
   │             ├─ slug
   │             ├─ title
   │             ├─ content (HTML)
   │             ├─ views
   │             └─ crawlerVisits
   │
   ├──→ Post (文章)
   │    ├─ id
   │    ├─ title
   │    ├─ slug
   │    ├─ content
   │    ├─ status
   │    └─ publishedAt
   │
   ├──→ Keyword (关键词)
   │    ├─ id
   │    ├─ keyword
   │    ├─ volume
   │    ├─ difficulty
   │    └─ rankings []
   │        │
   │        └──→ KeywordRanking (排名记录)
   │
   ├──→ Sitemap (网站地图)
   └──→ SpiderLog (爬虫日志)
        ├─ id
        ├─ ip
        ├─ userAgent
        ├─ bot (googlebot等)
        └─ createdAt

SpiderPoolSource (内容源)
├─ id
├─ name
├─ filePath
├─ paragraphs []
├─ headings []
└─ keywords []
```

---

## 核心技术栈

### 前端
- Next.js 14 (App Router)
- React 18
- TypeScript
- Tailwind CSS
- NextAuth.js (认证)
- React Hook Form + Zod (表单)
- Recharts (图表)

### 后端
- Next.js API Routes
- Prisma ORM
- PostgreSQL (Supabase)
- bcryptjs (密码加密)

### 部署
- Vercel (主站点)
- PM2 (Admin后台进程管理)
- Nginx (反向代理)
- 宝塔面板 (服务器管理)

### AI/工具
- OpenAI GPT-4 (内容优化)
- Cheerio (HTML解析)
- 中文分词 (关键词提取)

---

## 性能优化

### 1. 缓存策略

```
浏览器缓存
    ↓
Nginx缓存 (1-24小时)
    ↓
Next.js缓存 (ISR)
    ↓
数据库查询
```

### 2. CDN加速

- Vercel自带CDN (主站点)
- Cloudflare可选 (蜘蛛池)

### 3. 数据库优化

- Prisma连接池
- 索引优化
- 查询优化

---

## 安全设计

### 认证授权
- NextAuth.js Session
- 角色权限控制 (RBAC)
- API Key认证

### 数据安全
- 密码bcrypt加密
- 敏感设置AES加密
- SQL注入防护 (Prisma)
- XSS防护

### 访问控制
- 管理后台IP白名单 (可选)
- Rate Limiting
- CORS配置

---

## 监控和日志

### 应用监控
- PM2监控
- Next.js内置分析
- Vercel Analytics

### 爬虫监控
- User-Agent识别
- 访问日志记录
- 统计分析

### 错误追踪
- PM2日志
- Nginx日志
- Next.js错误边界

---

## 扩展性设计

### 水平扩展
- 蜘蛛池可无限添加域名
- PM2集群模式
- 数据库读写分离 (可选)

### 垂直扩展
- VPS升级配置
- 数据库性能优化
- CDN加速

---

## 总结

这是一个**集中管理、分布式部署**的SEO管理系统：

- ✅ **集中管理**: 所有内容在Admin后台统一管理
- ✅ **分布式部署**: 蜘蛛池分布在多个VPS，降低成本
- ✅ **动态生成**: 蜘蛛池页面通过API动态生成，实时更新
- ✅ **SEO优化**: 内链网络、Sitemap、爬虫友好
- ✅ **可扩展**: 轻松添加新域名、新内容源
- ✅ **低成本**: Vercel免费、VPS最低配置、Supabase免费套餐

**核心优势**: 无需在每个VPS上部署代码，只需配置Nginx反向代理即可！
