# ==========================================
# 🗄️ 数据库配置 (必须)
# ==========================================
# PostgreSQL 数据库连接字符串
#
# 本地开发：
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/seo_monorepo"
#
# 生产环境（Supabase）：
# DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres?schema=public&pgbouncer=true&connection_limit=1"
# DIRECT_URL="postgresql://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres"
#
# 格式说明：
# postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/seo_monorepo"

# Direct URL for migrations (不使用连接池)
# 生产环境必须设置，从 Supabase 项目设置中获取
DIRECT_URL="postgresql://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres"

# ==========================================
# 🔐 NextAuth 配置 (必须)
# ==========================================
# JWT 签名密钥（必须至少 32 字节）
# 生成方法: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here-change-this"

# 应用完整 URL（必须包含协议，不要末尾斜杠）
# 本地开发: http://localhost:3100
# 生产环境: https://admin.yourdomain.com
NEXTAUTH_URL="http://localhost:3100"

# ==========================================
# 🔑 系统设置加密密钥 (推荐)
# ==========================================
# 用于加密敏感设置（如 API Keys）
# 生成方法: openssl rand -base64 32
# 如果不设置，将使用默认密钥（不安全）
SETTINGS_ENCRYPTION_KEY="your-encryption-key-here-change-this"

# ==========================================
# 🗄️ Supabase Client 配置 (生产环境)
# ==========================================
# Supabase 项目 URL（用于存储和实时功能）
# 从 Supabase 项目设置中获取
NEXT_PUBLIC_SUPABASE_URL="https://xxxxx.supabase.co"

# Supabase 匿名密钥（公开密钥，用于客户端）
# 从 Supabase 项目设置 > API 中获取
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"

# ==========================================
# 🤖 AI API 配置 (可选)
# ==========================================
# OpenAI API Key（用于 AI SEO 优化功能）
# 优先级：数据库设置（/settings 页面） > 环境变量
# 推荐在管理后台 /settings 页面配置，无需在此设置
OPENAI_API_KEY="sk-your-openai-api-key"

# OpenAI 模型选择
# 可选: gpt-4-turbo, gpt-4, gpt-3.5-turbo
OPENAI_MODEL="gpt-4-turbo"

# Tavily API Key（用于网络搜索功能）
# 从 https://tavily.com 获取
TAVILY_API_KEY="tvly-your-api-key"

# Vercel AI Gateway Key（可选）
# 用于 AI 请求管理和优化
VERCEL_AI_GATEWAY_KEY="vck-your-gateway-key"

# ==========================================
# 🌐 网站配置
# ==========================================
# 管理后台
NEXT_PUBLIC_SITE_NAME="SEO 管理后台"

# 前台网站（如果部署）
NEXT_PUBLIC_WEBSITE1_URL="https://website1.yourdomain.com"
NEXT_PUBLIC_WEBSITE1_NAME="网站 1"

NEXT_PUBLIC_WEBSITE2_URL="https://website2.yourdomain.com"
NEXT_PUBLIC_WEBSITE2_NAME="网站 2"

NEXT_PUBLIC_WEBSITE_TG_URL="https://website-tg.yourdomain.com"
NEXT_PUBLIC_WEBSITE_TG_NAME="网站 TG"

# ==========================================
# 📊 Google Analytics (可选)
# ==========================================
# 可以在后台 /settings 页面配置，也可以在此设置
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"

# ==========================================
# 🔍 搜索引擎验证码 (可选)
# ==========================================
NEXT_PUBLIC_GOOGLE_VERIFICATION="your-google-verification-code"
NEXT_PUBLIC_BING_VERIFICATION="your-bing-verification-code"
NEXT_PUBLIC_YANDEX_VERIFICATION="your-yandex-verification-code"

# ==========================================
# 🚀 生产环境配置
# ==========================================
NODE_ENV="production"
PORT=3100

# ==========================================
# 📝 使用说明
# ==========================================
#
# 1. 本地开发：
#    - 复制此文件为 .env.local
#    - 填写本地数据库连接
#    - 设置 NEXTAUTH_URL=http://localhost:3100
#    - 运行: pnpm dev
#
# 2. 服务器部署：
#    - 在服务器上创建 .env.local 文件
#    - 配置生产数据库连接（PostgreSQL）
#    - 设置 NEXTAUTH_URL 为实际域名
#    - 设置 NODE_ENV=production
#    - 详见：服务器部署指南.md
#
# 3. 密钥生成：
#    openssl rand -base64 32
#
# 4. 环境变量优先级：
#    - API Keys：数据库设置（/settings） > 环境变量
#    - 其他配置：环境变量 > 默认值
#
# 5. 重要提示：
#    - 所有环境变量都在 turbo.json 的 globalEnv 中声明
#    - 添加新变量时，记得同时更新 turbo.json
#    - 生产环境中敏感信息务必使用强密码
#
# 6. 数据库配置（服务器部署）：
#    - 使用宝塔面板创建 PostgreSQL 数据库
#    - 数据库名：seo_websites
#    - 用户名：seo_user
#    - 设置强密码并记录
#    - 连接格式：postgresql://用户名:密码@localhost:5432/数据库名
