# Nginx configuration for autopushnetwork.xyz
# Spider Pool Domain - VPS 1

server {
    listen 80;
    listen [::]:80;
    server_name autopushnetwork.xyz www.autopushnetwork.xyz;

    # 日志
    access_log /www/wwwlogs/autopushnetwork.xyz-access.log;
    error_log /www/wwwlogs/autopushnetwork.xyz-error.log;

    # 主页和所有页面
    location / {
        set $slug $uri;

        # 反向代理到 Admin Backend
        proxy_pass https://adminseohub.xyz/api/p/autopushnetwork.xyz?slug=$slug;

        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 缓存设置（1小时）
        proxy_cache_valid 200 1h;
        proxy_cache_bypass $http_pragma $http_authorization;

        # 超时设置
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # Sitemap
    location = /sitemap.xml {
        proxy_pass https://adminseohub.xyz/api/p/autopushnetwork.xyz?type=sitemap;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        add_header Content-Type application/xml;
        proxy_cache_valid 200 24h;
    }

    # Robots.txt
    location = /robots.txt {
        proxy_pass https://adminseohub.xyz/api/p/autopushnetwork.xyz?type=robots;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        add_header Content-Type text/plain;
        proxy_cache_valid 200 24h;
    }

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/html text/css application/javascript application/xml application/json;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# SSL 配置（使用 certbot 自动生成）
# 运行: certbot --nginx -d autopushnetwork.xyz -d www.autopushnetwork.xyz
